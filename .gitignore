# Build output
/target/

# Models (too large for git — use scripts/download-models.sh)
/models/
**/*.onnx

# Shared libraries (compiled PAM module etc)
**/*.so

# Enrollment data (user-specific, sensitive)
*.bin
embeddings.bin

# IR emitter config (hardware-specific, generated by --install)
ir-emitter.toml

# Debug captures
ir_frame.png
*.png

# PAM module build artifacts
*.pp
*.mod

# OS / editor
.DS_Store
.vscode/
.idea/
*.swp
*.swo
*~

