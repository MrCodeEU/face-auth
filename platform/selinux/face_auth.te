module face_auth 1.0;

require {
    type xdm_t;
    type var_run_t;
    class sock_file write;
    class unix_stream_socket connectto;
}

# Allow SDDM (xdm_t) to connect to the face-auth daemon socket
allow xdm_t var_run_t:sock_file write;
allow xdm_t var_run_t:unix_stream_socket connectto;
